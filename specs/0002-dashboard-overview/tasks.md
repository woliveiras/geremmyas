# Tasks: Dashboard overview HTML generation

## CLI integration

- [x] Add `"dashboard"` case to `cli.go` switch
- [x] Define `--output` flag (default `.geremmyas/dashboard`)
- [x] Wire `runDashboard()` in `dashboard.go`: parse → render → write README

## HTML templates + assets

- [x] Create `dashboard_assets/` directory structure
- [x] Write `layout.html` with shared head, nav bar, and content block
- [x] Write `index.html` template (summary bar + family cards grid)
- [x] Write `family.html` template (phase groups + spec rows + progress bars)
- [x] Write `spec.html` template (rendered body + sidebar metadata + task
  progress)
- [x] Write `style.css` with dark/light theme, responsive grid, status badge
  colors
- [x] Add `go:embed` directive for `dashboard_assets/`

## Renderer

- [x] Implement `RenderDashboard(data DashboardData, outputDir string) error`
- [x] Generate `index.html` from overview template
- [x] Generate `families/<family-slug>/index.html` per family
- [x] Generate `specs/<NNNN>/index.html` per spec (with markdown → HTML body)
- [x] Copy CSS to output directory
- [x] Create output directory tree if it does not exist

## README auto-generation

- [x] Implement `GenerateReadme(data DashboardData) string`
- [x] Format: `<!-- geremmyas:generated -->` header + grouped flat list
- [x] Write to `specs/README.md` (overwrite)

## sdd template update

- [x] Update `project/templates/specs/README.md` to document that
  `specs/README.md` is auto-generated by `geremmyas dashboard`
- [x] Add frontmatter field documentation to template so new specs are
  dashboard-compatible

## Tests

- [x] Unit test: GenerateReadme with sample DashboardData
- [x] Unit test: template rendering produces valid HTML (no template errors)
- [x] Integration test: runDashboard with temp spec tree → verify output files
  exist and contain expected content
- [x] Integration test: --output flag writes to custom directory
- [x] Integration test: spec detail page includes dependency links
- [x] Visual check: open generated index.html, verify dark/light theme
