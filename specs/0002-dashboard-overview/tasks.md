# Tasks: Dashboard overview HTML generation

## CLI integration

- [ ] Add `"dashboard"` case to `cli.go` switch
- [ ] Define `--output` flag (default `.geremmyas/dashboard`)
- [ ] Wire `runDashboard()` in `dashboard.go`: parse → render → write README

## HTML templates + assets

- [ ] Create `dashboard_assets/` directory structure
- [ ] Write `layout.html` with shared head, nav bar, and content block
- [ ] Write `index.html` template (summary bar + family cards grid)
- [ ] Write `family.html` template (phase groups + spec rows + progress bars)
- [ ] Write `spec.html` template (rendered body + sidebar metadata + task
  progress)
- [ ] Write `style.css` with dark/light theme, responsive grid, status badge
  colors
- [ ] Add `go:embed` directive for `dashboard_assets/`

## Renderer

- [ ] Implement `RenderDashboard(data DashboardData, outputDir string) error`
- [ ] Generate `index.html` from overview template
- [ ] Generate `families/<family-slug>/index.html` per family
- [ ] Generate `specs/<NNNN>/index.html` per spec (with markdown → HTML body)
- [ ] Copy CSS to output directory
- [ ] Create output directory tree if it does not exist

## README auto-generation

- [ ] Implement `GenerateReadme(data DashboardData) string`
- [ ] Format: `<!-- geremmyas:generated -->` header + grouped flat list
- [ ] Write to `specs/README.md` (overwrite)

## sdd template update

- [ ] Update `project/templates/specs/README.md` to document that
  `specs/README.md` is auto-generated by `geremmyas dashboard`
- [ ] Add frontmatter field documentation to template so new specs are
  dashboard-compatible

## Tests

- [ ] Unit test: GenerateReadme with sample DashboardData
- [ ] Unit test: template rendering produces valid HTML (no template errors)
- [ ] Integration test: runDashboard with temp spec tree → verify output files
  exist and contain expected content
- [ ] Integration test: --output flag writes to custom directory
- [ ] Integration test: spec detail page includes dependency links
- [ ] Visual check: open generated index.html, verify dark/light theme
